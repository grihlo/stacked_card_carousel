import 'package:flutter_test/flutter_test.dart';
import 'package:stacked_card_carousel/stacked_card_carousel.dart';

void main() {
  test('init works', () {
    final stackedCardCarousel = Object(); //TODO write tests
    expect(stackedCardCarousel, isNotNull);
  });
}
